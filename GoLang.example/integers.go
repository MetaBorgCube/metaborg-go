package main;

import "fmt";
  
func main(){    
		x,y := 4,5;
		fmt.Println("x: ",x);
		x = x + y;
		fmt.Println("x = x + y",x );
		x = x - y;
		fmt.Println("x = x - y",x );
		x = x * y;
		fmt.Println("x = x * y" ,x);
		fmt.Println("What was y?" );
};       


                            